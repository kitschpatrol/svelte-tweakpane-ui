import Flags from './Flags.astro';

:::note[Dynamic Props]
**This component has dynamic props, which means the props available on the component can change depending on the value or type of _other_ props passed to the component.**

Props contingent on certain conditions are annotated below with a <Flags names={["dynamic"]} /> flag, and include explanations of the "conditions" affecting the prop's availability. [More Info&thinsp;→](https://www.example.com/TODO)
:::
