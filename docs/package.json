{
	"name": "svelte-tweakpane-ui-docs",
	"version": "1.0.0",
	"description": "Documentation website for Svelte Tweakpane UI.",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/kitschpatrol/svelte-tweakpane-ui.git"
	},
	"license": "MIT",
	"author": {
		"name": "Eric Mika",
		"email": "eric@ericmika.com",
		"url": "https://ericmika.com"
	},
	"sideEffects": false,
	"type": "module",
	"scripts": {
		"build": "tsx ./scripts/build.ts",
		"check": "astro check --tsconfig tsconfig.build.json",
		"clean": "rm -rf .astro && rm -rf dist",
		"dev": "astro dev",
		"preview": "poptab ; open https://localhost:8787/svelte-tweakpane-ui ; wrangler dev --show-interactive-dev-session false --https-cert-path ~/.vite-plugin-mkcert/cert.pem --https-key-path ~/.vite-plugin-mkcert/dev.pem",
		"start": "astro dev",
		"sync": "astro sync",
		"validate-links": "export NODE_EXTRA_CA_CERTS=\"$(mkcert -CAROOT)/rootCA.pem\" && pnpx linkinator https://localhost:8787/svelte-tweakpane-ui --recurse --verbosity error --retry-errors-count 2 --retry-errors true --retry true --concurrency 1 --timeout 10000 --retry-errors-jitter 50 --skip www.linkedin.com",
		"validate-links-live": "pnpx linkinator https://kitschpatrol.com/svelte-tweakpane-ui --recurse --verbosity error --retry-errors-count 2 --retry-errors true --retry true --concurrency 1 --timeout 10000 --retry-errors-jitter 50  --skip www.linkedin.com"
	},
	"dependencies": {
		"@astrojs/check": "^0.9.9",
		"@astrojs/starlight": "^0.41.3",
		"@astrojs/svelte": "^9.0.1",
		"astro": "^7.0.9",
		"github-slugger": "^2.0.0",
		"gray-matter": "^4.0.3",
		"sharp": "^0.35.3",
		"svelte-tweakpane-ui": "link:.."
	},
	"devDependencies": {
		"@types/semver": "^7.7.1",
		"astro-html-kit": "^0.5.0",
		"canvas": "^3.2.3",
		"execa": "^9.6.1",
		"linkinator": "^7.6.1",
		"marked": "^18.0.5",
		"poptab": "^1.1.1",
		"read-package-up": "^12.0.0",
		"semver": "^7.8.5",
		"svelte": "^5.56.4",
		"wrangler": "^4.107.1"
	},
	"engines": {
		"node": ">=22.12.0",
		"pnpm": ">=10.0.0"
	}
}
